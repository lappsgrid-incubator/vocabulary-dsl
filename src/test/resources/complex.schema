Datatypes {
	complex('morphology') {
	    sequence {
	        element type:'xs:string', name:'majorWordClass'
	        element name:'wordClass', type:'xs:string'
	        element name:'prefix', type:'xs:string'
	        element name:'suffix', type:'xs:string'
	        element name:'number', type:'xs:string'
	    }
	}
}

